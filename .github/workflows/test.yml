name: Tests

on:
  push:
    branches: [master, ci]

jobs:
  unit-test:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Install dependancies
      run: |
        sudo apt update
        sudo apt install -y cmake libboost-all-dev
    - name: make
      working-directory: ./test
      run: |
        mkdir build && cd build
        cmake ..
        make -j 4
        ctest -vv
  regression-test:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Install dependancies
      run: |
        sudo apt update
        sudo apt install -y cmake libboost-all-dev gcc g++ python3
    - name: make
      run: |
        sh docker/install.sh
        cd test/regression
        python3 regression.py
